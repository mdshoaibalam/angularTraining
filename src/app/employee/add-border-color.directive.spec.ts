import { AddBorderColorDirective } from './add-border-color.directive';

describe('AddBorderColorDirective', () => {
  it('should create an instance', () => {
  //  const directive = new AddBorderColorDirective();
    //expect(directive).toBeTruthy();
  });
});
